sudo: required
dist: xenial
language: python
cache: pip
python:
  - "3.6"
  - "3.7"
  - "3.8"
  - "nightly"
env:
  TOXENV=py

matrix:
  include:
    - python: 3.7
      env: TOXENV=docs

install:
  - pip install -U pip tox

script:
  - tox
